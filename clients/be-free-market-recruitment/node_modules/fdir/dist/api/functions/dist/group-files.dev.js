"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.build = build;

var groupFiles = function groupFiles(groups, directory, files) {
  groups.push({
    directory: directory,
    files: files,
    dir: directory
  });
};

var empty = function empty() {};

function build(options) {
  return options.group ? groupFiles : empty;
}